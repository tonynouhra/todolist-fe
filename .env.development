# Development environment variables
# Increase memory for TypeScript checking
TSC_COMPILE_ON_ERROR=true
GENERATE_SOURCEMAP=false

# Skip TypeScript checking in development for faster builds
# (TypeScript will still check in the IDE)
TSC_WATCHFILE=UseFsEvents